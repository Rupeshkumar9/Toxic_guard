runtime: python311

env_variables:
  DJANGO_SETTINGS_MODULE: toxiguard_project.settings

entrypoint: gunicorn -b :$PORT toxiguard_project.wsgi:application

handlers:
# Serve static files directly from the static folder
- url: /static
  static_dir: static

# Route all other traffic to the Django application
- url: /.*
  script: auto
